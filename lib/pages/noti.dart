import 'package:flutter/material.dart';

class UserNoti extends StatelessWidget {
  const UserNoti({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Center(child: Text("UserNoti"),),
    );
  }
}